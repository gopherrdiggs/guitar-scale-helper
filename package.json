{
  "name": "guitar-scale-helper",
  "private": true,
  "version": "0.0.1",
  "description": "guitar-scale-helper",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch"
  },
  "dependencies": {
    "@ionic/core": "4.1.1",
    "split-me": "^1.1.7"
  },
  "devDependencies": {
    "@stencil/core": "0.18.0"
  }
}
